{
  "name": "@coderifts/gateway-verifier",
  "version": "0.1.0",
  "description": "Reject an API request unless it carries a CodeRifts receipt that verifies and whose scope matches the request. Customer-hosted, offline, zero dependencies.",
  "license": "MIT",
  "main": "src/index.js",
  "files": ["src", "README.md", "LICENSE"],
  "engines": { "node": ">=18" },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/coderifts/gateway-verifier.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": { "test": "node --test test/*.test.js" },
  "dependencies": {}
}
